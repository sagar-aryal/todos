# Project MVP

```
Fetch the todos from https://jsonplaceholder.typicode.com/todos
The component to be broken down into atomic structure
Apply the CRUD functionality
C -> CREATE
R -> READ
U -> UPDATE
D -> DELETE
when the todo is create it should be pushed to the top of the array
```
